# Preserve client IP from an upstream reverse proxy (external nginx) when present;
# otherwise use the direct peer (e.g. local access to the compose frontend).
map $http_x_real_ip $auth_client_ip {
    default $http_x_real_ip;
    ""      $remote_addr;
}
